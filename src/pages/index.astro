---
import Layout from '@/layouts/Layout.astro';
import CardRenderer from '@/components/CardRenderer';
import { DEFAULT_CARD_DATA, encodeCardTemplatePreset, type CardData } from '@/lib/card';

const makeCard = (overrides: Partial<CardData>): CardData => ({
  ...DEFAULT_CARD_DATA,
  locale: 'zh',
  avatarType: 'emoji',
  avatarValue: '🤖',
  backgroundType: 'none',
  backgroundValue: '',
  qrcodeUrl: '',
  ...overrides,
});

const heroCard = makeCard({
  username: 'vibe.engineer',
  slogan: '发出来不是总结，是压迫感',
  totalTokens: 2_300_000_000,
  lastMonthTokens: 1_400_000_000,
  channel: 'claude',
  theme: 'brand-dark',
  platform: 'wechat',
  metaphorCategory: 'flex',
  projects: [
    { id: 'hero-github', name: 'GitHub', icon: '🐙', url: 'https://github.com', displayType: 'icon+text' },
    { id: 'hero-site', name: 'Portfolio', icon: '🌐', url: 'https://tokcard.dev', displayType: 'icon+text' },
    { id: 'hero-saas', name: 'TokCard', icon: '⚡', url: 'https://tokcard.dev', displayType: 'icon+text' },
  ],
  modelBreakdown: [
    { name: 'Claude', percentage: 62, color: '#d97706' },
    { name: 'GPT', percentage: 24, color: '#10b981' },
    { name: 'Cursor', percentage: 14, color: '#6529c4' },
  ],
});

const showcaseCards: CardData[] = [
  heroCard,
  makeCard({
    username: 'token.diva',
    avatarValue: '⚡',
    slogan: '别人晒腹肌，我晒 token',
    totalTokens: 780_000_000,
    lastMonthTokens: 420_000_000,
    channel: 'gpt',
    theme: 'gradient-dream',
    platform: 'xiaohongshu',
    metaphorCategory: 'meme',
    projects: [
      { id: 'diva-app', name: 'AI App', icon: '📱', url: 'https://tokcard.dev', displayType: 'icon+text' },
      { id: 'diva-ph', name: 'PH', icon: '🚀', url: 'https://tokcard.dev', displayType: 'icon+text' },
    ],
    modelBreakdown: [
      { name: 'GPT', percentage: 54, color: '#10b981' },
      { name: 'Claude', percentage: 28, color: '#d97706' },
      { name: 'Gemini', percentage: 18, color: '#ef4444' },
    ],
  }),
  makeCard({
    username: 'prompt.pm',
    avatarValue: '🧠',
    slogan: '我会的不是技术，是使唤 AI',
    totalTokens: 96_000_000,
    lastMonthTokens: 60_000_000,
    channel: 'deepseek',
    theme: 'brand-light',
    platform: 'twitter',
    metaphorCategory: 'worker',
    projects: [
      { id: 'pm-site', name: 'Website', icon: '🪪', url: 'https://tokcard.dev', displayType: 'icon+text' },
      { id: 'pm-case', name: 'Case Study', icon: '📊', url: 'https://tokcard.dev', displayType: 'icon+text' },
    ],
    modelBreakdown: [
      { name: 'DeepSeek', percentage: 48, color: '#1652f0' },
      { name: 'Claude', percentage: 32, color: '#d97706' },
      { name: 'GPT', percentage: 20, color: '#10b981' },
    ],
  }),
];

const showcaseMeta = [
  { platform: '朋友圈', note: '高压排面款' },
  { platform: '小红书', note: '有图有梗款' },
  { platform: 'X / LinkedIn', note: '专业名片款' },
];

// Pre-compute template URLs for showcase cards
const showcaseTemplateUrls = showcaseCards.map((card) => {
  const encoded = encodeCardTemplatePreset(card);
  return `/create?p=${encoded}`;
});
---

<Layout title="TokCard - 你的 AI 用量，值得一张名片" description="30 秒把你的 AI token 用量做成一张社交名片图。无需注册，导出即发朋友圈、小红书、X。" lang="zh">
  <main class="min-h-screen flex flex-col bg-[#fbfbfd] text-[#1d1d1f] overflow-x-hidden">
    <div class="absolute inset-0 -z-10 overflow-hidden pointer-events-none">
      <div class="absolute left-[-8rem] top-[-6rem] h-[24rem] w-[24rem] rounded-full bg-[#0071e3]/10 blur-3xl"></div>
      <div class="absolute right-[-6rem] top-[8rem] h-[22rem] w-[22rem] rounded-full bg-[#7c3aed]/10 blur-3xl"></div>
    </div>

    <!-- Nav -->
    <nav class="px-6 py-5 flex items-center justify-between max-w-6xl mx-auto w-full relative z-10">
      <span class="text-xl font-semibold tracking-tight">TokCard</span>
      <div class="flex items-center gap-3">
        <a href="/rank" class="hidden sm:inline text-sm font-medium text-[#64748b] hover:text-[#1d1d1f]">排行榜</a>
        <a href="#showcase" class="hidden sm:inline text-sm font-medium text-[#64748b] hover:text-[#1d1d1f]">看案例</a>
        <a href="/create" class="text-sm font-semibold px-4 py-2 rounded-full bg-[#0071e3] text-white shadow-lg shadow-[#0071e3]/20 hover:scale-[1.02] transition-all">
          立即生成
        </a>
      </div>
    </nav>

    <!-- Hero -->
    <section class="px-6 pt-8 pb-16 md:pt-10 md:pb-24 max-w-6xl mx-auto w-full">
      <div class="grid gap-14 lg:grid-cols-[minmax(0,1.05fr)_430px] lg:items-center">
        <div>
          <h1 class="text-5xl md:text-7xl font-semibold tracking-tight leading-[1.02]">
            你的 AI 用量，<br />
            <span class="bg-[linear-gradient(135deg,#0071e3_0%,#7c3aed_60%,#f59e0b_100%)] bg-clip-text text-transparent">
              值得一张名片。
            </span>
          </h1>

          <p class="mt-6 text-lg md:text-xl text-[#6b7280]">
            30 秒生成，导出即发朋友圈。
          </p>

          <div class="mt-8 flex flex-col sm:flex-row sm:items-center gap-4">
            <a href="/create"
              class="inline-flex items-center justify-center gap-2 px-8 py-4 rounded-full font-semibold text-lg bg-[#0071e3] text-white shadow-[0_18px_40px_rgba(0,113,227,0.28)] hover:scale-[1.02] active:scale-[0.98] transition-all">
              立即生成我的卡片
            </a>
            <a href="#showcase"
              class="inline-flex items-center justify-center gap-2 px-6 py-4 rounded-full font-medium text-lg bg-white border border-[#dbe4ff] text-[#1d1d1f] shadow-sm hover:border-[#0071e3] transition-all">
              看案例 ↓
            </a>
          </div>

          <div class="mt-5 text-sm text-[#6b7280]">无需注册 · 本地编辑 · 导出即发</div>
        </div>

        <div class="flex justify-center lg:justify-end">
          <div class="relative w-full max-w-[430px]">
            <div class="absolute inset-0 rounded-[42px] bg-[radial-gradient(circle_at_top,rgba(0,113,227,0.18),transparent_55%)] blur-2xl"></div>
            <div class="relative rotate-[-5deg] drop-shadow-[0_36px_80px_rgba(15,23,42,0.22)]">
              <CardRenderer data={heroCard} scale={0.72} renderId="landing-hero-card" />
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Showcase -->
    <section id="showcase" class="px-6 py-16 max-w-6xl mx-auto w-full border-t border-[#d2d2d7]/40">
      <div class="flex items-end justify-between gap-6 flex-wrap">
        <div>
          <h2 class="text-3xl md:text-4xl font-semibold tracking-tight">同一套产品，不同人设都能打。</h2>
          <p class="mt-2 text-[#6b7280]">选一个喜欢的模板，30 秒就能出图。</p>
        </div>
      </div>

      <div class="mt-10 grid gap-8 lg:grid-cols-3">
        {showcaseCards.map((card, index) => (
          <article class="group">
            <div class="rounded-[32px] border border-[#dbe4ff] bg-white p-4 shadow-[0_18px_40px_rgba(15,23,42,0.08)] transition-transform duration-300 group-hover:-translate-y-1">
              <div class="flex justify-center overflow-hidden rounded-[24px] bg-[#f8fafc] py-4">
                <CardRenderer data={card} scale={0.4} renderId={`landing-showcase-${index}`} />
              </div>
            </div>
            <div class="mt-4 flex items-center justify-between gap-4 text-sm">
              <div>
                <div class="font-semibold text-[#1d1d1f]">@{card.username}</div>
                <div class="mt-1 text-[#6b7280]">{showcaseMeta[index].platform} · {showcaseMeta[index].note}</div>
              </div>
              <a href={showcaseTemplateUrls[index]}
                class="rounded-full bg-[#0071e3] px-4 py-2 text-xs font-semibold text-white shadow-sm hover:opacity-95 transition-all">
                用这个模板
              </a>
            </div>
          </article>
        ))}
      </div>
    </section>

    <!-- Value Props -->
    <section class="px-6 py-12 max-w-6xl mx-auto w-full">
      <div class="grid gap-5 md:grid-cols-3">
        <div class="rounded-[24px] border border-[#dbe4ff] bg-white p-6 shadow-sm">
          <div class="text-2xl">📊</div>
          <div class="mt-3 text-lg font-semibold text-[#1d1d1f]">看你的 AI 排名</div>
          <p class="mt-2 text-sm text-[#6b7280] leading-relaxed">自动生成档位徽章，从"百万俱乐部"到"十亿殿堂"，一眼看出你有多硬核。</p>
        </div>
        <div class="rounded-[24px] border border-[#dbe4ff] bg-white p-6 shadow-sm">
          <div class="text-2xl">🔗</div>
          <div class="mt-3 text-lg font-semibold text-[#1d1d1f]">展示你的项目作品</div>
          <p class="mt-2 text-sm text-[#6b7280] leading-relaxed">GitHub、博客、产品链接直接上卡，让别人扫一下就能找到你的作品。</p>
        </div>
        <div class="rounded-[24px] border border-[#dbe4ff] bg-white p-6 shadow-sm">
          <div class="text-2xl">🧠</div>
          <div class="mt-3 text-lg font-semibold text-[#1d1d1f]">看高手在做什么</div>
          <p class="mt-2 text-sm text-[#6b7280] leading-relaxed">烧了那么多 token 的人在做什么项目？扫码就知道，学习参考一步到位。</p>
        </div>
      </div>
    </section>

    <!-- Bottom CTA -->
    <section class="px-6 py-20 max-w-5xl mx-auto w-full border-t border-[#d2d2d7]/40 text-center">
      <h2 class="text-4xl md:text-5xl font-semibold tracking-tight leading-tight">把你的 AI 排面，发出去。</h2>
      <p class="mt-5 text-lg text-[#6b7280]">
        30 秒出图，无需注册，导出即发。
      </p>
      <div class="mt-8">
        <a href="/create"
          class="inline-flex items-center justify-center px-8 py-4 rounded-full font-semibold text-lg bg-[#0071e3] text-white shadow-[0_18px_40px_rgba(0,113,227,0.28)] hover:scale-[1.02] active:scale-[0.98] transition-all">
          立即生成
        </a>
      </div>
    </section>

    <!-- Footer -->
    <footer class="px-6 py-8 text-[13px] max-w-6xl mx-auto w-full text-[#86868b] border-t border-[#d2d2d7]/30">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
        <p>TokCard — 你的 AI 用量，值得一张名片</p>
        <p>10+ 主题 · 6 平台比例 · AI 文案增强 · 无需注册</p>
      </div>
    </footer>
  </main>
</Layout>
