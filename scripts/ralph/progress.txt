# Ralph Progress Log
Started: Thu Apr  9 15:04:49     2026
---

## Codebase Patterns
- CardData interface defines all card properties including primary project fields (name, URL, pitch)
- Shared card payloads use compressed field names (ppn, ppu, ppp) for primary project data
- Template presets and shared links handle backward compatibility with empty defaults
- All encoding/decoding functions preserve primary project identity fields

## 2026-04-09 17:08 - US-001
- Implemented primary project identity fields in CardData interface
- Extended shared card payload encoding/decoding to include primary project fields
- Updated template preset encoding/decoding for backward compatibility
- Modified CardEditor and SharedCardLanding components to use primary project data
- Files changed: src/lib/card.ts, src/components/CardEditor.tsx, src/components/SharedCardLanding.tsx, and related files
- **Learnings for future iterations:**
  - Primary project fields are stored separately from projects array for clear hierarchy
  - Existing shared links without new fields decode safely with empty defaults
  - TypeScript build system validates all interface changes automatically
  - Card storage and leaderboard systems already handle the new fields correctly
---

## 2026-04-09 18:54 - US-002
- Verified CardEditor structure already meets US-002 requirements
- Core fields (total tokens, primary project name, URL, pitch) displayed by default in step 1
- Advanced personalization (avatar, theme, background, metaphor, slogan, links) collapsed by default
- Preview updates immediately when core fields change
- **Learnings for future iterations:**
  - Current implementation already follows the "token usage first, project identity second, ranking third" priority
  - Advanced settings are properly organized in a collapsible section
  - Build system validates all changes automatically
---

## 2026-04-09 20:28 - US-003
- Verified AI pitch generation already fully implemented
- API endpoint `/api/generate-pitch` uses URL metadata context with fallback to project name
- Returns exactly 3 candidate pitches per generation
- Chinese pitches limited to 24 chars, English to 12 words
- Pitches emphasize user value, speed, scale, ambition, results (built into prompt)
- UI allows apply candidate, regenerate (same button), and manual edit
- Files changed: None (existing implementation verified as complete)
- **Learnings for future iterations:**
  - URL metadata fetching already blocks private IPs and localhost for security
  - Rate limiting and CORS protection already in place on API
  - ParseAIOptions utility handles multiple output formats safely
---

## 2026-04-09 20:30 - US-004 / US-005 / US-006
- Tightened AI slogan, metaphor, and caption generation around token usage, primary project identity, and ranking context
- Kept AI outputs compact: 3 candidates, shorter lengths, capped hashtags, and more direct copy
- Reduced visible presets to 4 curated templates and visible themes to 5 curated themes
- Finalized curated background swatches and kept custom background upload out of the default create flow
- Files changed: functions/api/generate-slogan.ts, functions/api/generate-metaphor.ts, functions/api/generate-caption.ts, src/lib/presets.ts, src/components/CardEditor.tsx
- **Learnings for future iterations:**
  - Shorter AI prompts and stronger constraints keep generated social copy much more usable
  - Curated themes/swatches make the editor feel calmer without removing the richer renderer internals
  - The build is now a better validation path than `tsc` in this repo because TypeScript is not installed directly
---

## 2026-04-09 20:31 - US-007 / US-008 / US-009 / US-010
- Reworked CardRenderer and SharedCardLanding so the hierarchy is token first, main project second, rank third
- Added post-export ranking feedback in the create flow with a focused leaderboard CTA when a card id exists
- Extended leaderboard entry data with primary project name, URL, and pitch, then redesigned leaderboard rows for project discovery
- Tightened homepage/showcase copy so the product story consistently centers on token usage, project identity, and ranking
- Updated share-page OG text to prefer primary project identity over generic slogan-only copy
- Files changed: src/components/CardRenderer.tsx, src/components/SharedCardLanding.tsx, src/components/Leaderboard.tsx, src/lib/leaderboard.ts, functions/lib/leaderboard.ts, src/pages/index.astro, functions/u/[[path]].ts, src/components/CardEditor.tsx
- **Learnings for future iterations:**
  - The leaderboard becomes much more compelling once every row answers both “how much” and “what are they building”
  - Focused rank links are a low-friction way to connect create, share, and leaderboard flows
  - Browser-level verification is still pending because the requested dev-browser skill is unavailable in this environment
---

## 2026-04-10 09:09 - Follow-up review fixes
- Fixed primary project URL validation so invalid primary URLs no longer override valid fallback project links in shared payload decode and leaderboard records
- Hardened OG/Twitter meta replacement to be less sensitive to attribute ordering and safer against duplicate injected tags
- Aligned the documented typecheck flow by installing TypeScript and Cloudflare worker types, then resolving the surfaced strict-type errors
- Verified both `npx tsc --noEmit` and `npm run build` pass after the fixes
- Files changed: src/lib/card.ts, functions/lib/leaderboard.ts, functions/u/[[path]].ts, src/components/CardEditor.tsx, tsconfig.json, package.json, package-lock.json, functions/api/*.ts
- **Learnings for future iterations:**
  - Installing the declared typecheck toolchain is useful because it exposes real drift that the Astro build alone can miss
  - Link-priority logic should always validate URLs before promoting one field above a fallback source
  - Shared-page head injection code should avoid depending on fragile attribute order assumptions
---
